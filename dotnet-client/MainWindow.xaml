<Window x:Class="DigitlexViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Digitlex Viewer" WindowStyle="None" WindowState="Maximized" Background="#000">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="80"/>
    </Grid.RowDefinitions>

    <!-- Header / Status -->
    <DockPanel Background="#001529" Height="40">
      <TextBlock x:Name="StatusText" Foreground="White" Margin="12,0" VerticalAlignment="Center"/>
      <Button Content="⚙" Width="36" Height="24" Margin="8" HorizontalAlignment="Right"
              Click="OpenSettings_Click"/>
    </DockPanel>

    <!-- Content area -->
    <Grid Grid.Row="1" Background="#000">
      <Image x:Name="ImageView" Stretch="Uniform" Visibility="Collapsed"/>
      <MediaElement x:Name="VideoView" LoadedBehavior="Manual" UnloadedBehavior="Stop" Visibility="Collapsed"/>
      <WebBrowser x:Name="HtmlView" Visibility="Collapsed"/>
    </Grid>

    <!-- Bottom ticker -->
    <Border Grid.Row="2" Background="#111" BorderBrush="#ffd700" BorderThickness="1">
      <ScrollViewer VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Hidden">
        <TextBlock x:Name="TickerText" Foreground="#ffd700" FontSize="18" Margin="16,24"/>
      </ScrollViewer>
    </Border>
  </Grid>
</Window>

